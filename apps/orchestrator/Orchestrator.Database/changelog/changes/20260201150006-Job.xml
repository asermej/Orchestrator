<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="20260201150006-create-jobs-table" author="system">
        <createTable tableName="jobs">
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="organization_id" type="uuid">
                <constraints nullable="false" foreignKeyName="fk_jobs_organization" references="organizations(id)"/>
            </column>
            <column name="job_type_id" type="uuid">
                <constraints nullable="true" foreignKeyName="fk_jobs_job_type" references="job_types(id)"/>
            </column>
            <column name="external_job_id" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="title" type="varchar(500)">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="text">
                <constraints nullable="true"/>
            </column>
            <column name="status" type="varchar(50)" defaultValue="active">
                <constraints nullable="false"/>
            </column>
            <column name="location" type="varchar(255)">
                <constraints nullable="true"/>
            </column>
            <!-- Audit fields -->
            <column name="created_at" type="timestamp" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamp">
                <constraints nullable="true"/>
            </column>
            <column name="created_by" type="varchar(100)">
                <constraints nullable="true"/>
            </column>
            <column name="updated_by" type="varchar(100)">
                <constraints nullable="true"/>
            </column>
            <column name="is_deleted" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="deleted_at" type="timestamp">
                <constraints nullable="true"/>
            </column>
            <column name="deleted_by" type="varchar(100)">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <createIndex tableName="jobs" indexName="idx_jobs_organization_id">
            <column name="organization_id"/>
        </createIndex>

        <createIndex tableName="jobs" indexName="idx_jobs_job_type_id">
            <column name="job_type_id"/>
        </createIndex>

        <createIndex tableName="jobs" indexName="idx_jobs_external_id">
            <column name="organization_id"/>
            <column name="external_job_id"/>
        </createIndex>

        <createIndex tableName="jobs" indexName="idx_jobs_status">
            <column name="status"/>
        </createIndex>

        <createIndex tableName="jobs" indexName="idx_jobs_is_deleted">
            <column name="is_deleted"/>
        </createIndex>

        <addUniqueConstraint tableName="jobs" columnNames="organization_id, external_job_id" constraintName="uk_jobs_org_external_id"/>
    </changeSet>

</databaseChangeLog>
